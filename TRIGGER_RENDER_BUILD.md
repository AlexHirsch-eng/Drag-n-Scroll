# Render Build Trigger

This file triggers a new build on Render to deploy the latest authentication and learning fixes.

## Latest Changes to Deploy:
1. Auto-login after registration
2. Fixed `/user/me/` endpoint
3. Auto-create course if missing
4. Complete learning plan integration

Build should start automatically in 1-2 minutes after this push.
