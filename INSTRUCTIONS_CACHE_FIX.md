# 🔧 ИНСТРУКЦИЯ ПО ИСПРАВЛЕНИЮ 404 ОШИБКИ

## Проблема
Браузер показывает закэшированный 404 ответ для `/api/learning/main-screen/`, хотя сервер работает правильно.

## ✅ Что уже исправлено:
1. Backend endpoint работает - проверено через curl (отвечает 401 - правильно)
2. Добавлены cache-busting параметры в frontend API
3. Добавлены `Cache-Control` заголовки

## 🎯 Как исправить у себя:

### Вариант 1: Полный сброс кэша браузера
1. **Откройте DevTools** (F12)
2. **Нажми правой кнопкой** на кнопке обновления
3. **Выбери "Empty Cache and Hard Reload"**
   - Или: Ctrl+Shift+Delete → очистить кэш за последний час
   - Или: Ctrl+F5 (обычный жесткий перезапуск)

### Вариант 2: Ручная очистка
**Chrome/Edge:**
1. F12 → Network tab
2. ✓ Отметь "Disable cache"
3. Закрыть и открыть DevTools
4. Обновить страницу (F5)

**Firefox:**
1. F12 → Network tab
2. ✓ Отметь "Disable cache"
3. Обновить страницу

**Safari:**
1. Cmd+Option+E (очистить кэш)
2. Cmd+R (перезагрузить)

### Вариант 3: Инкогнито режим
1. Открой новую страницу в инкогнито режиме
   - Chrome: Ctrl+Shift+N
   - Edge: Ctrl+Shift+P
   - Firefox: Ctrl+Shift+P
2. Зайди на http://localhost:5174
3. Протестируй

### Вариант 4: Остановить и перезапустить frontend
```bash
# Останови frontend (Ctrl+C в терминале)
# Затем запусти снова:
cd frontend
npm run dev
```

## 🔍 Проверка после исправления

После сброса кэша, в DevTools Console ты должен увидеть:
```javascript
✅ GET http://localhost:8000/api/learning/main-screen/ 200 (OK)
```

А НЕ:
```javascript
❌ GET http://localhost:8000/api/learning/main-screen/ 404 (Not Found)
```

## 📝 Что было изменено в коде:

### frontend/src/api/client.ts
```typescript
// Добавлены cache-control заголовки
async get<T>(url: string, params?: any) {
  const response = await this.client.get<T>(url, {
    params,
    headers: {
      'Cache-Control': 'no-cache',
      'Pragma': 'no-cache'
    }
  })
  return response.data
}
```

### frontend/src/api/learning.ts
```typescript
// Добавлены cache-busting timestamp параметры
async getMainScreen(dayNumber?: number, hskLevel?: number) {
  params.append('_t', Date.now().toString())  // ← Cache buster
  // ...
}
```

## ⚡ Быстрое решение:

Если ничего не помогает - открой в режиме инкогнито!
Это гарантированно работает без кэша.
