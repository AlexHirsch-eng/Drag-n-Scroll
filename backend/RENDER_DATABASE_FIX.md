# 🔧 Как исправить базу данных на Render Dashboard

## Проблема
Render использует старый DATABASE_URL который указывает на Supabase (aws-1-ap-south-1.pooler.supabase.com) вместо новой PostgreSQL базы.

## Решение (шаги с картинками)

### Шаг 1: Открой Render Dashboard
1. Перейди на https://dashboard.render.com
2. Войди в аккаунт

### Шаг 2: Найди сервис
1. В левом меню выбери "Web Services"
2. Найди и кликни на **drag-n-scroll**

### Шаг 3: Открой Environment Variables
1. В верхней части экрана найди вкладку **"Events"**
2. Рядом с ней должны быть **"Environment"** или **"Variables"**
3. Кликни на неё

### Шаг 4: Найди DATABASE_URL
1. В списке переменных найди **DATABASE_URL**
2. Он должен выглядеть примерно так:
   ```
   postgres://postgres:[PASSWORD]@aws-1-ap-south-1.pooler.supabase.com...
   ```

### Шаг 5: Удали старый DATABASE_URL
**ВАЖНО**: Нажми кнопку **Delete** (или **X**, или **🗑️**) напротив DATABASE_URL
- НЕ просто отредактируй
- А именно УДАЛИ полностью эту переменную

### Шаг 6: Сохранить изменения
Нажми **"Save Changes"** или **"Apply Changes"** или **"Deploy"**

### Шаг 7: Подожди пересоздания базы
1. В левом меню выбери **"Databases"**
2. Найди **drag-n-scroll-db**
3. Если она есть - удали её (нажми три точки → Delete)
4. Render автоматически создаст новую базу с новым render.yaml

### Шаг 8: Проверь
Через 2-3 минуты открой:
```
https://drag-n-scroll.onrender.com/api/health/
```

Должен показать:
```json
{
  "database_url": "postgres://drag_n_scroll_user:****@dpg-......oregon-postgres.render.com/..."
}
```

Если видишь **supabase.com** - вернись к Шагу 5 и убедись что удалил старую переменную!

### Шаг 9: Примени миграции
Открой в браузере:
```
POST https://drag-n-scroll.onrender.com/api/migrate/
```

Или через терминал:
```bash
curl -X POST https://drag-n-scroll.onrender.com/api/migrate/
```

## Готово!
Теперь можешь залогиниться: https://drag-n-scroll.vercel.app/
