# Как добавить видео в проект

## Способ 1: Добавить одно видео через команду

Самый простой способ - добавить видео по одному:

```bash
cd backend
python add_video.py path/to/your/video.mp4
```

Если есть превью-картинка:
```bash
python add_video.py path/to/your/video.mp4 path/to/your/thumbnail.jpg
```

## Способ 2: Папка для массового добавления

### Шаг 1: Подготовьте видео

1. Создайте папку для своих видео:
   - Windows: `backend/media/videos/`
   - Или любая другая папка

2. (Опционально) Создайте превью-картинки:
   - Папка: `backend/media/video_thumbnails/`
   - Формат: JPG, PNG, WebP
   - Имя файла должно совпадать с видео (например: `lesson1.mp4` → `lesson1.jpg`)

### Шаг 2: Добавьте видео

```bash
cd backend

# Способ A: Добавить одно видео из папки media/videos/
python add_video.py media/videos/my_video.mp4 media/video_thumbnails/my_video.jpg

# Способ B: Добавить все видео из папки (через Django shell)
python manage.py shell
>>> exec(open('add_videos_from_folder.py').read())
```

## Способ 3: Через Django Admin

1. Запустите сервер:
   ```bash
   cd backend
   python manage.py runserver
   ```

2. Откройте http://localhost:8000/admin

3. Войдите (создайте суперюзера, если нужно):
   ```bash
   python manage.py createsuperuser
   ```

4. Перейдите в "Video" → "Add Video"

5. Заполните поля:
   - Video File: выберите файл видео
   - Thumbnail: выберите превью (опционально)
   - Description: описание видео
   - Duration: длительность в секундах
   - Status: выберите "Ready"
   - Is Featured: поставьте галочку

## Поддерживаемые форматы видео

- `.mp4` (рекомендуется)
- `.mov`
- `.avi`
- `.mkv`
- `.webm`

## Поддерживаемые форматы превью

- `.jpg` / `.jpeg`
- `.png`
- `.webp`

## Пример использования

```bash
# 1. Положите видео в папку
copy my_lesson.mp4 backend\media\videos\
copy my_lesson.jpg backend\media\video_thumbnails\

# 2. Добавьте в базу данных
cd backend
python add_video.py media/videos/my_lesson.mp4 media/video_thumbnails/my_lesson.jpg

# 3. Видео появится в приложении!
# Откройте http://localhost:5173 и перейдите в раздел Videos
```

## Рекомендации

1. **Размер видео**: До 100 МБ (можно изменить в settings.py)
2. **Длительность**: 15 секунд - 5 минут
3. **Размер превью**: 1080x1920 px (вертикальный, как TikTok)
4. **Формат видео**: MP4 с H.264 кодеком

## Быстрая проверка

После добавления видео, проверьте что всё работает:

```bash
# Проверьте количество видео в базе
python manage.py shell
>>> from video_app.models import Video
>>> print(f"Всего видео: {Video.objects.count()}")
>>> for video in Video.objects.all():
>>>     print(f"- {video.id}: {video.video_file.name}")
```

## Просмотр видео

После добавления видео появятся:
- В разделе "Videos" на фронтенде (http://localhost:5173)
- В ленте видео
- В профиле создателя

Наслаждайтесь! 🎥
